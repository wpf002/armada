export * from './group-name';

/** Placeholder — Phase 1 fills in Zod schemas, shared types, and the
 *  `visibleFieldsFor(viewer, subject)` permission function with full tests. */
export const ARMADA_SHARED_VERSION = '0.0.0';
